// to use for editing the details 

export default function edit(field, details) {
    switch(field) {
        case "name":
            // get a new name from the details
            break;
        case "time":
            // get the new open and close time
            break;
        case "noStories":
            // get the new no_of_stories
            break;
        case "occupancy":
            // get the new total_occupancy
            break;
        case "services":
            // get the new services provided
            break;
        default:
            break;
    }
}